name: Deploy to DigitalOcean Spaces

on:
  workflow_dispatch:

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Not configured
        run: |
          echo "This repository is a React Native mobile app."
          echo "Spaces upload is not configured for mobile builds."
