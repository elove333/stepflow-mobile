name: DigitalOcean Deploy (Manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Not configured
        run: |
          echo "This repository is a React Native mobile app."
          echo "DigitalOcean App Platform deploy is not configured for mobile builds."
